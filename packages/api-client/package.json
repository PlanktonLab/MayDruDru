{
  "name": "@maydru/api-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "由 OpenAPI 產生的型別 + fetch 包裝；前後端契約以 OpenAPI 為準",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "typecheck": "tsc -b",
    "lint": "oxlint src",
    "test": "vitest run"
  },
  "devDependencies": {
    "openapi-typescript": "^7.9.1",
    "typescript": "~6.0.2",
    "vitest": "^3.2.4"
  }
}
